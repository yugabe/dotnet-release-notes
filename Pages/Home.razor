@page "/"
@inject NavigationManager NavigationManager

<PageTitle>Redirecting...</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("release-notes?fromHome=1", replace: true);
    }
}
